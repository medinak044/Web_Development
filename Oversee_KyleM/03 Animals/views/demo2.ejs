<h1>Demo2 page</h1>

<% data.forEach((element)=> { %>
    <h2>
        <%= element.name %>
    </h2>
    <% }) %>

        <% for( let i=0; i < data.length; i++ ) { %>
            <h2>
                <%=data[i].breed %>
            </h2>
            <% } %>